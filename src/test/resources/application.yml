spring:
  application:
    name: MunsalCaseStudy
    description: Case Study for Mert UNSAL
  profiles:
    include: postgresql, kafka
springdoc:
  api-docs:
    path: /api-docs

server:
  port: 8080

logging:
  level:
    org.hibernate: INFO
    org.liquibase: INFO